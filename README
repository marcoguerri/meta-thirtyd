# Building the container

sudo docker build -t <...>/oe_env .

# Running the container

sudo docker run  -u root --rm --cap-add NET_ADMIN --device /dev/net/tun:/dev/net/tun -ti <name>

# Running the relay

sudo /usr/sbin/dhcrelay -d -i docker0 <DHCP_SERVER> -U <OUTGRESS_INTERFACE>


